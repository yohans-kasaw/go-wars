package main

import (
	"fmt"
)

// func dotest(t, k int, ls []int, expected int) {
// 	fmt.Println(ChooseBestSum(t, k, ls), expected, t)
// }

func Expect(res int) {
	fmt.Println(res)
}

func main() {
	TestFunc()
}
